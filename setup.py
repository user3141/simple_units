from setuptools import setup, find_packages

setup(name='simple_units',
      version='0.1',
      description='Add units to Python',
      packages=find_packages(exclude=['test*']),
      install_requires=[]
     )
